
name: Run E2E Tests on Label
on:
  pull_request:
    types:
      - labeled

jobs:
  run-e2e-tests:
    if: contains(github.event.label.name, 'run-e2e')
    runs-on: ubuntu-latest

    steps:
      - name: Install dependencies
        run: exit 1
